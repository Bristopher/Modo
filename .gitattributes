* text=auto

# Shell scripts must always have LF endings, even when checked out on Windows.
# A CRLF shebang (`#!/usr/bin/env sh\r`) makes the kernel look for `sh\r` and the
# container entrypoint dies with `env: 'sh\r': No such file or directory`.
*.sh text eol=lf
*.bash text eol=lf
